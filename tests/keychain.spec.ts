import { Keychain } from "../src/utils/keyhain";

describe('Keychain', () => {
    it('Set/Get', () => {
        const keychain = new Keychain('test');
        // keychain.set('key', { value: 'value' });
    });
});
